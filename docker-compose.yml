services:
  vpt-dreamer:
    build: .
    image: vpt-dreamer:v2
    platform: linux/amd64
    container_name: vpt_dreamer_dev
    environment:
      - DISPLAY=:99
      - MINEDOJO_HEADLESS=1
      - LIBGL_ALWAYS_SOFTWARE=1
    volumes:
      - ./models:/workspace/models
      - ./src:/workspace/src
      - ./logs:/workspace/logs
    ports:
      - "6006:6006"
    stdin_open: true
    tty: true
